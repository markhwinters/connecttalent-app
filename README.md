# ConnectTalent

ConnectTalent is an ephemeral professional networking application. Swipe, match, and connect before time runs out.

## Instructions
- Start the server from the `server` directory.
- Start the client from the `client` directory.
